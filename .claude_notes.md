# Claude Development Notes

## Current Session (2025-10-25)

### Major Integration - SparkCAN Library
1. **Replaced ros2_control with Direct CAN Communication**
   - Removed ros2_control framework and socketcan_interface dependencies
   - Integrated sparkcan library for direct SparkFlex motor controller communication
   - Created custom C++ driver node (`sparkflex_driver_node.cpp`)
   - Eliminates complex hardware interface plugin (sparkflex_ros2_control didn't exist)

2. **SparkFlex Driver Node**
   - Direct CAN communication via sparkcan library
   - Velocity control mode (RPM-based)
   - Publishes odometry (`/odom`) and joint states (`/joint_states`)
   - Subscribes to `/cmd_vel` for navigation commands
   - 50 Hz update rate with automatic heartbeat
   - Differential drive kinematics with proper unit conversions

3. **Configuration Simplified**
   - All parameters now in launch file (no complex YAML configs)
   - Motor CAN IDs: 1 (FL), 2 (FR), 3 (RL), 4 (RR)
   - Wheel parameters: 0.7620m separation, 0.1778m radius
   - PID tuning via launch parameters

4. **Documentation Updates**
   - Fixed all ODrive references → SparkFlex
   - Updated HARDWARE_SETUP.md with SparkFlex configuration
   - Created SPARKCAN_INTEGRATION.md with detailed architecture
   - Updated can_setup.sh comments

5. **Build System Updates**
   - CMakeLists.txt: C++17, sparkcan dependency, C++ node compilation
   - package.xml: Removed ros2_control deps, added sparkcan
   - Added hardware directory to install targets

6. **Important Notes**
   - sparkcan only works with firmware 24.0.X (NOT 25.0.X)
   - Use REV Hardware Client to configure motors and set CAN IDs
   - Motors must receive periodic heartbeat or they disable

### Current Status
- ✅ SparkCAN library integrated
- ✅ Custom driver node created and compiles
- ✅ Launch file updated for new architecture
- ✅ All ODrive references removed
- ✅ Documentation updated
- ⚠️ Untested on hardware (needs sparkcan installation and motor configuration)

### File Changes This Session
- `src/sparkflex_driver_node.cpp` - NEW: Custom driver node
- `CMakeLists.txt` - Added C++ compilation, sparkcan dependency
- `package.xml` - Replaced ros2_control with sparkcan
- `launch/hardware_bringup.launch.py` - Complete rewrite for sparkcan
- `HARDWARE_SETUP.md` - Removed ODrive, added SparkFlex instructions
- `hardware/can/can_setup.sh` - Updated comments
- `hardware/SPARKCAN_INTEGRATION.md` - NEW: Integration documentation

### Deprecated Files (No Longer Used)
- `urdf/lunabot_hardware.urdf.xacro` - ros2_control config
- `config/control/hardware/hardware_controllers.yaml` - ros2_control config
- `hardware/sparkflex/motor_params.yaml` - Replaced by launch params
- `hardware/can/sparkflex_can_ids.yaml` - Replaced by launch params

### Next Steps
1. Install sparkcan library on Raspberry Pi: `sudo apt install sparkcan`
2. Configure SparkFlex controllers with REV Hardware Client (firmware 24.0.X)
3. Build and test: `colcon build --packages-select lunabot_one`
4. Test motor communication: `ros2 launch lunabot_one hardware_bringup.launch.py`
5. Verify odometry and tune PID if needed

---

## Previous Session (2025-10-04)

### Major Improvements - Navigation & Costmaps
1. **Costmap Configuration**
   - Configured local and global costmaps to use `/scan_merged` (all 3 LIDARs combined)
   - Fixed local costmap dynamic obstacle detection with proper update rates (5Hz update, 2Hz publish)
   - Extended LIDAR obstacle detection range to 8m (from 2.5m)
   - Extended raytrace clearing range to 8m for better obstacle clearing

2. **Robot Footprint**
   - Replaced circular `robot_radius: 0.45` with accurate rectangular footprint
   - Footprint: `[[0.55, 0.38], [0.55, -0.38], [-0.55, -0.38], [-0.55, 0.38]]` (1.1m x 0.76m)
   - Helps prevent false obstacle detection from robot's own body

3. **LIDAR Wheel Clipping Fix**
   - Reduced LIDAR angle range from 127° to 100° (±0.8727 radians)
   - Prevents LIDARs from detecting robot's own wheels during turns
   - Modified `description/lidar.xacro` for all 3 LIDARs (front, left, right)

4. **Obstacle Avoidance Tuning**
   - Reduced inflation radius from 0.75m to 0.50m for tighter navigation through narrow spaces
   - Allows robot to navigate closer to obstacles while maintaining safety

5. **SLAM Mapping System**
   - Created `launch/slam_launch.py` with SLAM Toolbox
   - Configured for 8m max range with merged LIDAR scans
   - Aggressive loop closure settings to fix drift/ghosting issues
   - Added lifecycle manager for auto-start
   - Created `arena_boulder_map` using SLAM with boulders

6. **Map Management**
   - All launch files now use `arena_map.yaml` as default
   - Removed `usc_arena` map files (consolidated to arena_map)
   - Fixed map origin alignment: Gazebo coordinates now match map coordinates
   - Map origin shifted by (+1, +1) so initial pose (1,1) aligns with Gazebo (1,1)

7. **Makefile Enhancements**
   - Added `make slam` - Launch SLAM mapping
   - Added `make save-map` - Save current SLAM map with custom name
   - Added `make load_map` - Load arena map for visualization
   - Added `make load_boulder_map` - Load arena boulder map
   - Updated `make full-nav` to use arena_boulder_map

8. **Visual Improvements**
   - Made waypoint markers nearly invisible in Gazebo
   - Reduced from 0.1m tall cylinders to 0.01m flat discs
   - Semi-transparent (alpha 0.3) with dimmer green color
   - Positioned at ground level (z=0.005m)

### Current Status
- ✅ Full navigation working with global and local costmaps
- ✅ Local costmap updates dynamically as robot moves
- ✅ LIDAR wheel clipping issue resolved
- ✅ SLAM mapping functional with loop closure
- ✅ Map coordinates aligned with Gazebo world
- ✅ Tight navigation possible with reduced inflation radius

### File Changes This Session
- `config/params/nav2_params.yaml` - Costmap config, footprint, scan_merged topics, inflation radius
- `description/lidar.xacro` - Reduced LIDAR FOV to 100°
- `description/face.xacro` - Fixed duplicate material definitions (from previous session)
- `launch/slam_launch.py` - New SLAM mapping launch file
- `maps/arena_boulder_map.yaml` - New map with adjusted origin
- `Makefile` - Added SLAM commands, updated nav commands
- `worlds/usc_arena.world` - Reduced waypoint marker visibility
- Multiple launch files updated to use arena_map.yaml as default

### Known Issues
- SLAM may have slight drift if loop closure doesn't trigger (mitigated with aggressive settings)

### Next Steps
- Test arena waypoint navigation with new boulder map
- Monitor local costmap performance during navigation
- Consider further inflation radius reduction if needed for tighter turns

---

## Previous Session (2025-10-04)

**Summary**: Comprehensive repository cleanup and professional documentation rewrite
- Updated hardware configuration from ODrive to SparkFlex motor controllers
- Fixed broken URDF references and standardized 4-wheel drive configuration
- Simplified README files for team members with little ROS2 experience
- Removed AI-generated language patterns while maintaining technical accuracy
- Added .gitignore and cleaned up unnecessary files
